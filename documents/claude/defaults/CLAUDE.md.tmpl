# {{PROJECT_NAME}}
*EPS Framework (eps-workflow) — Configured for {{STACK}}*

## EPS Workflow
```bash
/research    /innovate    /design --srs    /design --basic
/design --detail    /plan    /execute    /validate
```

## Quality Gates
| Q1 | ≥80% evidence | Q2 | Unique IDs | Q3 | Bilingual ≥60% | Q4 | Interfaces only |

## Architecture
{{STACK_RULES}}

## Key Paths
- `core/cli/ops.js` — CLI entry (14 ops)
- `config/project-config.json` — Project config
- `rules/` — Path-scoped rules

*Generated by `npx eps init`*
