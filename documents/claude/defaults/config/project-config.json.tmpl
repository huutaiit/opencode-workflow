{
  "projectName": "{{PROJECT_NAME}}",
  "techStack": {
    "backend": {
      "stack": "{{STACK}}",
      "variant": "{{VARIANT}}"
    }
  },
  "sourceRoots": [],
  "rag": {
    "server": "{{RAG_SERVER}}",
    "embedding": "{{EMBEDDING_SERVER}}"
  }
}
